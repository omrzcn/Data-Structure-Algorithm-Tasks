package com.cydeo;

public class Node {
    int id;

    Node next;   // i will store adress of the next element. With this variable, i refer nex=t element.

    public Node(int id) { // i created constructor to use this Node in other class.
        this.id = id;
    }

}
